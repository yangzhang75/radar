你是船用雷达安卓显控项目（送 IMO CAT 1 型式认证）的员工 AI，负责任务 **T2.2-refine：用 IEC 62288 精修 ColorMapper（认证级颜色）**。

> 这是对你(absorbed-stetson / `feat/ui-color`)已交付 T2.2 的**回炉精修**。继续在**同一分支 `feat/ui-color`** 上做，不要新建分支(你是该 package 的唯一负责人)。

## 上下文
你之前交付的 `ColorMapper` 把色相锚定到 IEC 62388 §7.3.1 并标了 `TODO(待标准:62288)`，因为当时缺 62288。**现在 IEC 62288 Ed.2 已入库**：`~/Desktop/雷达开发资料/80-733_IEC 62288 Ed.2_2014_FDIS.pdf`。请据此把占位色值精确化。合规追溯 ID：DISP-03。

## 工作规则（铁律）
1. 在已有 worktree `wt-ui-color`、分支 `feat/ui-color` 上继续。**只改 `ui-core/` 下 `com.shipradar.uicore.color`**。不碰 shared、其他 package、根 gradle。
2. 必须带单元测试，`./gradlew :ui-core:test` 全绿。**改完立即 commit。**
3. **送认证**：每个颜色/亮度值 KDoc 标注 `IEC 62288 §x.x`(精确到条款/表)；62288 仍未给的 `TODO(待标准)`。DISP-03 落实条款写进交付报告(勿改矩阵文件)。

## 实现要求（精修，不破坏现有 API/测试）
- 从 62288 提取并精确落实：**昼/黄昏/夜 调色板的具体颜色与亮度**(背景、回波层级、文本/图形前景色)；回波强度色阶；**多普勒接近/远离的标准色**(若 62288/62388 指定)。
- 用 62288 的精确值替换之前的占位/锚定值，逐条改 KDoc 引用为 62288 §条款；移除已解决的 `TODO(待标准:62288)`。
- 保持公共 API(`amplitudeColor`/`dopplerColor`/`colorTable`/调色板枚举)稳定，T2.1 渲染端已依赖。
- 把"对照标准断言具体 ARGB"的单测补齐/更新到 62288 值。

## 验证
- 单测：关键色值断言到 62288 条款给定值；三调色板覆盖；接近/远离色不同且符合标准。

## 交付报告
改动文件、测试结果、DISP-03 已落实的 62288 §条款行(替代原占位)、62288 仍未覆盖的缺口、疑问。
