# 任务台账（Live） — 第一波

> 编排者每轮更新。状态：⬜待派 / 🔨进行 / 📦已提交待验 / ✅验证通过 / 🔀已合入integration。
> 验证 = 隔离 worktree 跑模块测试 + 文件级未越界审查。

## 第一波交付状态（截至 2026-06-07 晚）
| 任务 | 员工会话 | 分支 | 独占 package | 提交 | 状态 |
|---|---|---|---|---|---|
| T1.2 辐条解析 | rounded-fireplace | `feat/comms-image` | `comms.halo.image` | `2955140` | 🔀 **已合入** integration |
| T1.3 控制+状态 | thorn-poppyseed | `thorn-poppyseed` | `comms.halo.control/.status` | `ad1ddcf` | 🔀 **已合入**（测试断言对照文档字节示例） |
| T1.5 NMEA解析 | past-freon | `feat/comms-serial` | `comms.iec61162` | `8a07d64` | 🔀 **已合入**（含AIS解码+重组，校验和丢弃） |
| T2.1 PPI几何 | vivacious-clover | `feat/ui-ppi` | `uicore.ppi` | `c9a9417` | 🔀 **已合入** |
| T2.2 着色 | absorbed-stetson | `feat/ui-color` | `uicore.color` | `60ac1aa` | 🔀 **已合入**（精确色待62288） |
| T2.3 CPA/TCPA+融合 | few-basin | `feat/ui-target` | `uicore.target` | `8ca2c54` | 🔀 **已合入**（对照A.823算例） |
| T0.3 假数据发生器 | observant-aura | `feat/halofeed` | `halofeed` | `8863355` | 🔀 **已合入**（员工写完但忘提交→编排者验证后提交合并，30测试） |

**集成结果**：**7/7** 合入 `integration@408a730`，`./gradlew build` 全绿，**206 测试 0 失败**，跨模块编译通过、零合并冲突。

## 审查记录（严谨）
- 全部 6 个分支**文件级未越界**（`git show --stat` 确认仅改各自 package，未碰 `shared/` 或构建文件）。
- 测试质量抽查：T1.3 断言**文档原文字节**（6nm→`03C1 10B2 0100` 等）；T2.3 断言 **A.823 Appendix-2 算例**（head-on 8NM@20kn→CPA 0/TCPA 24min）；T1.5 校验和+坏包丢弃+§条款引用。
- 发现并确认的协议细节：HALO `03C1` 量程字段文档标 `range_m`，实际按**分米(m×10)**上线（唯一与文档示例一致的解释），T1.3 已正确处理并注释。

## 待办
- **T0.3 halofeed 未交付** —— 离线端到端联调(喂数据→comms)需要它。需重新催派/重派，或编排者接手。
- 第二波（Android 外壳/渲染/UI）仍等 §6 #3 设备型号。

## 集成顺序（§6.4）
shared(冻结) → comms-core(T1.2/T1.3/T1.5/+T1.4待) → ui-core(T2.1/T2.2/T2.3) → T0.3喂数据联调 → 第二波Android。
