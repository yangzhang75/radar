# 任务台账（Live） — 第一波

> 编排者每轮更新。状态：⬜待派 / 🔨进行 / 📦已提交待验 / ✅验证通过 / 🔀已合入integration。
> 验证 = `tools/review-branch.sh <branch> <:module:test>` 全绿 + 文件级未越界。

| 任务 | 员工会话 | 分支 | 独占 package | 最新提交 | 状态 |
|---|---|---|---|---|---|
| T0.3 假数据发生器 | observant-aura | `feat/halofeed` | `halofeed` | (基线) | 🔨 进行 |
| T1.2 辐条解析 | rounded-fireplace | `feat/comms-image` | `comms.halo.image` | `2955140` | ✅ 验证通过(14测试，交叉核对SDK头) |
| T1.3 控制+状态 | thorn-poppyseed | `feat/comms-ctl`(待建) | `comms.halo.control/.status` | (基线) | 🔨 进行(尚未提交分支) |
| T1.4 450传输(可选第8人) | — | `feat/comms-450` | `comms.iec450` | — | ⬜ prompt 就绪未派 |
| T1.5 NMEA解析 | past-freon | `feat/comms-serial` | `comms.iec61162` | (基线) | 🔨 进行 |
| T2.1 PPI几何 | vivacious-clover | `feat/ui-ppi` | `uicore.ppi` | (基线) | 🔨 进行 |
| T2.2 着色 | absorbed-stetson | `feat/ui-color` | `uicore.color` | `60ac1aa` | ✅ 验证通过(14测试；精确色待62288) |
| T2.3 CPA/TCPA+融合 | few-basin | `feat/ui-target` | `uicore.target` | (基线) | 🔨 进行 |

## 集成顺序（§6.4）
1. shared（已冻结，基线）。
2. comms-core 解析链：T1.2 → T1.3 → T1.5 → T1.4（互不重叠，可并入同一 `integration`）。
3. ui-core：T2.1 / T2.2 / T2.3（互不重叠）。
4. T0.3 假数据 → 用于喂 comms-core 跑通离线联调。
5. 第二波 Android 外壳（T1.1/T1.6/T2.4–2.9）合最后，待设备决策。

## 防重复/越界检查（每轮）
- 各分支 `git show --stat` 确认只改本任务 package；越界即叫停。
- T1.4 与 T1.5 边界：T1.4 只出 raw 语句串，T1.5 只解析串内字段——零重叠。
- shared/ 任何改动＝违规（接口已冻结）。

## 已验证交付
- T1.2 `feat/comms-image@2955140` — `:comms-core:test` 绿。
- T2.2 `feat/ui-color@60ac1aa` — `:ui-core:test` 绿。
