# 功能待办清单（Feature Backlog）

> 编排者维护。对照 IEC 62388 §3.5 功能要求 + Furuno 标准 + 用户提出项。状态:✅完成 / 🔨部分 / ⬜未做 / ⛔阻塞(需标准或硬件)。

## A. 纯软件可做(现在就能推)
| 功能 | 状态 | 说明 |
|---|---|---|
| **SIM/LIVE 数据模式**(模拟作为功能 + 明显标识) | ✅ | PPI 横幅 + 右栏切换(S 键);key(live) 切换清回波;模拟器验证 |
| **快捷键定义**(范围±/EBL/VRM/捕获/取消/模式) | ✅ | RadarHotkeys 控制级键 + Help 浮层(F1/?);模拟器验证 |
| **数据接口端口分模拟/实际 + 高实时性数据服务** | ✅ | DataInterfaceProfile + HaloEndpointSet(端口隔离);RealtimeIngest 提升优先级线程池;RadarCommsService profile-aware |
| 应用 LIVE 路径绑定前台 RadarCommsService(而非内联引擎) | ⬜ | 内联引擎已用实时 scope;完整方案=bindService 消费 binder.dataBus |
| 试操船(trial maneuver)UI | ✅ | W6-A feat/w6-trial 已并;面板+CPA重算,Y 键浮层,模拟器验证 |
| 平行索引线(PI lines)UI | 🔨 | 交互模型有 PiLine,缺操作 UI |
| 过去航迹(past tracks)开关/时长 UI | 🔨 | 叠加层有航迹,缺控制 |
| 目标报警圈/捕获区 UI | 🔨 | 有 guard zone 设置命令,缺图形操作 |
| 昼/黄昏/夜 主题切换 + 亮度(brilliance) | ✅ | W6-B feat/w6-theme 已并;K 键,全局 chrome 换色验证(PPI 回波色随主题=后续) |
| 偏心显示(off-center)/真运动复位 UI | ⬜ | 交互模型有 offCenter |
| 自动捕获(auto-acquisition)UI | ⬜ | ui-core 有占位 |
| 性能监视(performance monitor)/BITE | ✅ | W6-C feat/w6-bite 已并;B 键,链路/通道/传感器/整体健康,模拟器验证 |

## B. 较大功能
| 功能 | 状态 | 说明 |
|---|---|---|
| **双量程画面(Radar B)** | ✅ | CommsRouter.echoSpokesB + onHaloImageB;并排双 PPI(A 6NM/B 1.5NM);D 键;模拟器验证 |
| 海图叠加(S-57/S-52 chart overlay,可裁剪) | ⛔ | 需 S-57 海图 + S-52 显示库,大工程 |

## C. 阻塞(需外部输入)
| 功能 | 阻塞于 |
|---|---|
| 颜色/符号像素级认证 | IHO S-52 + IEC 62288 原文(采购) |
| AIS 静态数据解码(船名/尺寸/航次) | ITU-R M.1371-5(采购) |
| 真雷达回波/真数据端到端 | 网络版 HALO 雷达 + 蒲公英 X5(硬件) |
| 320mm 物理显示实测 | 真机面板(硬件) |
| 业务阈值(CPA/TCPA/容量验收/捕获准则) | 张建/船级社 |
| 型式试验取证 | 实船 + 船级社 |

## 已完成(本会话)
PPI 渲染/多普勒着色/目标+矢量+航迹/CPA-TCPA/融合/量程·运动·定向/增益·海浪·雨雪·IR·转速控制/报警圈设置/BAM 报警状态机/触屏·键·鼠交互(基础)/EBL·VRM(线+读数)/右侧信息栏/PPI 数据框/三区标准布局/真解析管线(SIM)。

## 优先级建议
1. A 类逐个做(SIM/LIVE → 快捷键 → 主题切换 → PI线/试操船/航迹 UI)。
2. B 类双量程(中等);海图叠加(大,可后置)。
3. C 类等你采购标准 + 接硬件。
