# 认证缺口清单（Certification Gap List）

> 由第四波审计(W4-A..F)在代码中留下的 `TODO(待...)` 标记实测收割而成,按"谁能解"分类。
> 这是从"软件代码完成"走到"可型式认证"的待办总表。配合 `合规追溯矩阵.md` 使用。日期:2026-06-09。

## A. 需补充标准原文（采购/索取即可解,纯软件阻塞）
| 缺口 | 标准 | 影响 | 涉及文件 |
|---|---|---|---|
| 回波/符号**精确色度**(chromaticity) | **IHO S-52**(IEC 62288 §4.5.1 + §5.4.1.1 NOTE 把精确色委托给 S-52) | DISP-03 颜色认证级精度 | `uicore.color.ColorMapper`、`app.ppi.PpiView`、`uicore.target.OverlayScene` |
| AIS 报文解码(静态/航次,类型5/24) | **ITU-R M.1371-5 §3.3** | AIS 目标静态数据(船名/尺寸/航次) | `comms.iec61162.AisPayloadDecoder`/`AisReassembler`、VDM/VSD/SSD 解析 |
| 多普勒着色具体色相 | 厂商 HMI 约定(62288 **无**多普勒条款) | 接近/远离色合规依据 | `ColorMapper`(magenta 占位) |
> 注:IEC 62288 已入库;`uicore.ppi.RangeRings` 里一条"62288 缺失"TODO 是旧的,W4-B 未清理 → 下轮顺手清。

## B. 需真机/真雷达抓包（必须上硬件,软件已留接口+stub）
| 缺口 | 说明 | 涉及文件 |
|---|---|---|
| HALO **目标通道 wire 格式**(236.6.7.18/.19) | docx 无字节布局;已建模+映射函数+stub,等真机抓包或 SDK 源 | `comms.halo.target.TargetParser` |
| HALO **跟踪控制**(236.6.7.20)Acquire/Cancel 字节 | 同上,编码器留 stub | `comms.halo.target.TrackControlEncoder`、`comms.halo.control.HaloControlEncoder` |
| HALO **握手字段语义**(0x0010 视频/0x0012 ARPA 组码等) | 部分按观测值推断,需真机确认 | `comms.halo.handshake.HandshakeModel`/`HaloHandshake` |
| **320mm 有效显示区实测** | 几何换算已就绪(DisplaySize),需真实面板 DPI/物理尺寸验证 | `uicore.ppi.DisplaySize`、`app.ppi.PpiView`(DISP-01) |

## C. 需张建/船级社/型式试验参数（业务输入,做到验收时定值）
| 缺口 | 说明 | 涉及文件 |
|---|---|---|
| CPA/TCPA 危险阈值默认值 | 操作员可设,但验收试验可能固定具体值 | `uicore.target.Danger` |
| 雷达/AIS 融合关联门限 | 按 IEC 62388 Tables 28–34 在验收时标定 | `uicore.target.Fusion` |
| 目标容量验收阈值 | 确认验收测试门限(≥40/40/200/240) | `uicore.target.Capacity` |
| 自动捕获选择准则 | A.823 §3.2.2/§3.3 / GB §4.2.2.2 的优先级细则 | `uicore.target.AutoAcquisition` |
| 目标安卓设备型号/系统版本 | 定 minSdk/targetSdk 与 320mm 实测前提(已默认 28/34) | 全局 |

## D. 软件可补（不阻塞,需要时实现）
- **61162-1 待补语句**(已建框架+§条款 TODO,约 20 条):TLB/TLL/OSD/RSD/DDC/HBT/ACN/ALC/ALR/SSD/VSD/VBW/HDG 等(§8.3.x)。主力 7 条(HDT/GGA/RMC/VTG/VDM/TTM/ALF)已实现。
- **AIS 类型5/24 静态解码**:需先在 `contract` 加静态字段(船名/尺寸),再按 M.1371 解。
- **昼/黄昏/夜调色板暗化**:DAY 已精确(62288),DUSK/NIGHT 暗化曲线待补(`app.alarm.AlarmColors`)。
- **报警状态 RESPONSIBILITY_TRANSFERRED 接入 61162 ACN/ARC**(`Iec61162Parser` §8.3.7)。

## 认证战役剩余大块(非软件,需统筹)
1. **真机/真雷达 + 蒲公英 VPN 端到端联调**(解 B 类大部分)。
2. **型式试验取证**:320mm 实测、ARPA 现场精度(A.823/GB 11711 实船)、报警状态机实测、符号/颜色第三方比对。
3. **认证文档包**:把 `合规追溯矩阵.md` 扩成正式符合性声明 + 测试报告 + 证据归档。

> 下一步建议:A 类补两份标准(S-52、ITU-R M.1371)即可解锁颜色/AIS 的认证级精度(纯软件);B/C 类等硬件与张建输入。
